# LyOS
A custom x86_64 monolithic kernel and OS written from scratch in C and Assembly. Features a native TCP/IP stack, Intel E1000 drivers, and a built-in web browser with a custom HTML/CSS rendering engine. No external libraries, just pure code.
